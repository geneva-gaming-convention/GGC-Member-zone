<%= form_for [@event_resource.event, Registration.new],:html => {:class => 'form-horizontal'} do |f| %>
<%= hidden_field_tag 'event_resource_id', @event_resource.id %>
<div class="row">
  <div class="col-md-4">
    <h2>1) Select a <%= @event_resource.event.shortname %>pack</h2>
    <div class="form-group">
      <% if @event_resource.event.event_packs.each do |pack| %>
      <div class="div-radio">
        <div class="row title-pack">
          <div class="col-md-8">
            <h2><%= pack.name.capitalize %></h2>
          </div>
          <div class="col-md-4 text-right">
            <h3><%= pack.price %> CHF</h3>
          </div>
        </div>
        <div class="div-description">
          <%= pack.description.html_safe %>
        </div>
        <%= radio_button_tag(:registration_pack, pack.id) %>
      </div>
      <% end.empty? %>
      <div class="panel panel-danger">
        <div class="panel-heading">
          <h3 class="panel-title">Nope nope nope</h3>
        </div>
        <div class="panel-body">
          There's actually no available packs for this event.
        </div>
      </div>
      <% end %>
    </div>
  </div>
  <div class="col-md-4">
    <h2>2) Choose your team</h2>
    <div class="form-group">
      <div class="col-md-12">
        <% if current_logged_user.get_teams_by_game(@event_resource.game).each do |team| %>
        <div class="div-radio">
          <div class="row title-pack">
            <div class="col-md-8">
              <h2><%= team.name.capitalize %></h2>
            </div>
            <div class="col-md-4 text-right">
              <h3><%= team.users_group.name %></h3>
            </div>
          </div>
          <ul class="list-group">
            <% team.team_members.each do |teammate| %>
            <li class="list-group-item">
              <%= teammate.user.name.capitalize %>
              <%= teammate.user.lastname.capitalize %>
            </li>
            <% end %>
          </ul>
          <%= radio_button_tag(:registration_team, team.id) %>
        </div>
        <% end.empty? %>
        <div class="panel panel-danger">
          <div class="panel-heading">
            <h3 class="panel-title">Nope nope nope</h3>
          </div>
          <div class="panel-body">
            <p>
              <h4>I'm a player</h4>
              It's a team based game dude, you need to have a team to participate.</br>
              <%= link_to "Create a team in one of my friend's group.", user_users_groups_path(current_logged_user) %>
            </p>
            <p>
              <h4>Hooo, sorry, you're maybe a manager ?</h4>
              <div class="div-radio">
                <div class="row ">
                  <div class="col-md-12 title-pack">
                    <h4>Yes 😎 , proud to support my team !</h4>
                  </div>
                  <div class="col-md-12">
                    <p>
                      You can only carry teams that are in the <%= link_to "same friend's groups", user_users_groups_path(current_logged_user) %> as you.</br>
                      Select the team you wan't to support:
                      <%= select_tag "registration_team", options_from_collection_for_select(current_logged_user.get_manageable_teams_by_game(@event_resource.game), "id", "name"), :class => "form-control" %>
                    </p>
                  </div>
                </div>
                <%= radio_button_tag(:is_manager, "true") %>
              </div>
            </p>
          </div>
        </div>
        <% end %>
      </div>
    </div>
  </div>
  <div class="col-md-4">
    <h2>3) Payment</h2>
    <div class="form-group">
      <div class="col-md-12 text-center">
        <button type="submit" class="btn btn-primary btn-lg">Go to payment</button>
      </div>
    </div>
  </div>
</div>
<% end %>
